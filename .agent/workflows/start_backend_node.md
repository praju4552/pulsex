---
description: Start the Project Templates Backend
---

1. Navigate to the backend directory
   cd d:\edu(1)\backend-node

2. Install dependencies (if not already installed)
   npm install

3. Start the server
   npm run dev

Note: Ensure PostgreSQL is running and DATABASE_URL is set in .env
